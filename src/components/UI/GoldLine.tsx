"use client";

const GoldLine = () => (
  <div
    className="my-0 h-2 w-full"
    style={{ background: "linear-gradient(90deg, transparent, rgba(201,169,110,0.3), transparent)" }}
  />
);

export default GoldLine;
